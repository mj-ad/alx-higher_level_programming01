#!/usr/bin/python3
def multiple_returns(sentence):
    if len(sentence) > 0:
        length = len(sentence)
        first = sentence[0]
        tuple_c = (lenght, first)
        return tuple_c
    else:
        lenght = 0
        first = None
        tuple_c = (lenght, first)
        return tuple_c
