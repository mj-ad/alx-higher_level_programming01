#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    if len(my_list) == 0:
        c = len(my_list) - 1
        my_list.sort()
        max_value = my_list[c]
        return max_value
    
